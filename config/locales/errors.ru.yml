ru:
  dry_validation:
    rules:
      password_confirmation: Подтверждение пароля
      password: Пароль
      passwords: Пароли
      name: Название
      budget_cents: Бюджет
      sport_kind: Вид спорта
      position_kind: Игровая позиция
      active_chips: Активные бонусы
      shirt_number: Номер игрока
      form: Форма
      description: Описание
      first_name: Имя
      last_name: Фамилия
      nickname: Прозвище
      reason: Причина
      status: Статус
      notification_type: Тип уведомления
    errors:
      user:
        password_length: должен быть больше или равняться %{length} символам
        banned: забанен
        rules:
          email:
            filled?: не может быть пустым
            invalid: имеет неверный формат
          password:
            filled?: не может быть пустым
          password_confirmation:
            filled?: не может быть пустым
          passwords:
            different: должны совпадать
      fantasy_team:
        rules:
          name:
            filled?: не может быть пустым
          budget_cents:
            filled?: должен быть указан
      fantasy_league:
        rules:
          name:
            filled?: не может быть пустым
      cup:
        rules:
          name:
            filled?: не может быть пустым
            invalid: не может быть пустым
      league:
        rules:
          name:
            filled?: не может быть пустым
            invalid: не может быть пустым
          sport_kind:
            filled?: должен быть указан
            invalid: неверное значение
      injury:
        rules:
          reason:
            filled?: не может быть пустым
            invalid: неверное значение
          status:
            filled?: не может быть пустым
      player:
        rules:
          first_name:
            filled?: не может быть пустым
            invalid: не может быть пустым
          last_name:
            filled?: не может быть пустым
            invalid: не может быть пустым
          nickname:
            filled?: не может быть пустым
          position_kind:
            filled?: должен быть указан
            invalid: неверное значение
      feedback:
        rules:
          description:
            filled?: не может быть пустым
      season:
        rules:
          name:
            filled?: не может быть пустым
      lineup:
        active_chips_duplicates: не должны содержать дубликаты
      teams_player:
        rules:
          shirt_number:
            filled?: должен быть указан
            invalid: неверное значение
          form:
            filled?: должна быть указана
            invalid: неверное значение
