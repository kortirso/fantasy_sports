<%= render PageWrappers::PageComponent.new do %>
  <div class="grid grid-cols-1 xl:grid-cols-10 gap-8">
    <div class="xl:col-span-7">
      <div class="p-4 bg-white border border-stone-300 rounded">
        <span class="badge-dark inline-block">
          Week <%= @periodable.position %>
        </span>
        <h1 class="mb-2">Результаты недели</h1>
        <p class="mb-4">На этой странице представлены результаты игр и набранные игроками очки на текущей активной неделе.</p>
        <div class="flex flex-col md:flex-row justify-between mt-2 bg-stone-200 border border-stone-300 rounded mb-4">
          <div class="flex flex-row md:flex-col items-center justify-center md:justify-between flex-1 py-2 px-10 border-b md:border-b-0 md:border-r border-stone-300">
            <p class="text-center">Всего очков</p>
            <p class="ml-4 md:ml-0 text-xl">0</p>
          </div>
          <div class="flex flex-row md:flex-col items-center justify-center md:justify-between flex-1 py-2 px-10 border-b md:border-b-0 md:border-r border-stone-300">
            <p class="text-center">Средний результат</p>
            <p class="ml-4 md:ml-0 text-xl">0</p>
          </div>
          <div class="flex flex-row md:flex-col items-center justify-center md:justify-between flex-1 py-2 px-10">
            <p class="text-center">Лучший результат</p>
            <p class="ml-4 md:ml-0 text-xl">0</p>
          </div>
        </div>
        <div class="flex justify-between items-center mb-4">
          <div>
            <% if @periodable.position > 1 %>
              <%= link_to 'Previous', oracul_url(@oracul.uuid, week: @periodable.position - 1), class: 'btn-primary btn-small' %>
            <% end %>
          </div>
          <div></div>
          <div>
            <%= link_to 'Next', oracul_url(@oracul.uuid, week: @periodable.position + 1), class: 'btn-primary btn-small' %>
          </div>
        </div>
        <% if @oracul.oracul_place.season? %>
          <%= react_component 'OraculWeek', uuid: @periodable.uuid, lineupUuid: @oraculs_lineup.uuid %>
        <% else %>
        <% end %>
      </div>
    </div>
    <div class="xl:col-span-3">
    </div>
  </div>
<% end %>
